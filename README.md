# Magic Schedule Builder

To rebuild the macOS application bundle you need a framework build of Python installed. Run the following command in a terminal on macOS:

```bash
python3 setup.py py2app
```

This uses py2app to create `dist/Magic Schedule Builder.app` with the necessary Tk frameworks embedded.
